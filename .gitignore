# Rust / Cargo
target/
**/*.rs.bk
Cargo.lock.bak

# Node / npm / pnpm / yarn
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm
.yarn-integrity

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/schemas/
**/dist/
**/build/
*.app/
*.dmg

# Android / Gradle
*.apk
*.aab
*.keystore
local.properties
.gradle/
captures/
.externalNativeBuild/
.cxx/

# IDE / editors
.vscode/
.idea/
*.swp
*.swo
*.iml
.fleet/
.zed/

# OS junk
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
$RECYCLE.BIN/
*.lnk

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Logs / temp
*.log
*.log.err
*.tmp
tmp/
.cache/
.inst*
.tmp-*

# Release artifacts (built binaries staged for upload to GitHub releases)
release-artifacts/

# Claude Code — se versiona el sistema (.claude/skills, hooks, settings.json);
# los overrides personales de cada máquina no.
.claude/settings.local.json

# Tauri Android generated project — regenerable with `tauri android init`.
# We DO commit a handful of files inside (MainActivity, MillenniumService,
# manifest, network config, build.gradle) by re-adding them explicitly.
src-tauri/gen/android/.gradle/
src-tauri/gen/android/build/
src-tauri/gen/android/app/build/
src-tauri/gen/android/buildSrc/build/
src-tauri/gen/android/keystore.properties
